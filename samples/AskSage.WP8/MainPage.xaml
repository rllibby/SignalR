<phone:PhoneApplicationPage 
    x:Class="AskSage.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"    
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Background="Black">
            <TextBlock x:Name="Title" Text="Ask Sage" Foreground="White" VerticalAlignment="Stretch"/>
            <ProgressBar x:Name="progress" Height="20" IsEnabled="True" IsIndeterminate="true" />
        </StackPanel>
        <ListBox x:Name="conversation" Grid.Row="1" Background="White" ItemsSource="{Binding Items}" >
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5,5,5,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border CornerRadius="4" BorderThickness="1" HorizontalAlignment="{Binding Alignment}" BorderBrush="Black">
                            <TextBlock x:Name="userinput" Margin="5,5,5,5" Text="{Binding Text}" Foreground="{Binding TextColor}" MaxWidth="400" Grid.Column="0" HorizontalAlignment="{Binding Alignment}" TextWrapping="Wrap" VerticalAlignment="Stretch"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBox x:Name="request" Grid.Row="2" Background="White" IsEnabled="False" VerticalAlignment="Bottom" KeyDown="Request_KeyDown" BorderBrush="Black" />
    </Grid>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBar.Buttons>
                <shell:ApplicationBarIconButton x:Name="ask" IsEnabled="false" Text="ask" Click="ask_Click" IconUri="/Toolkit.Content/ApplicationBar.Check.png" />
                <shell:ApplicationBarIconButton x:Name="speak" IsEnabled="false" Text="speak" Click="speak_Click" IconUri="/Toolkit.Content/microphone.png" />
            </shell:ApplicationBar.Buttons>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem x:Name="clear" Text="clear history" Click="clear_Click" />
                <shell:ApplicationBarMenuItem x:Name="speech" Text="don't read aloud" Click="speech_Click" />
                <shell:ApplicationBarMenuItem x:Name="demo" Text="demo off" Click="demo_Click" />
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>